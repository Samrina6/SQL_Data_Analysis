# SQL_Data_Analysis
SQL Data Analysis using MySQL on an E-commerce dataset. Includes SQL queries, dataset, and query output screenshots demonstrating SELECT, JOINs, GROUP BY, subqueries, views, indexes, and query optimization.
